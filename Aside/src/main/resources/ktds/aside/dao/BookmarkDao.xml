<?xml version="1.0" encoding="UTF-8"?>
<!--  -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ktds.aside.dao.BookmarkDao">

	
	<select id="selectBookmarkList" parameterType="int" resultType="Diary">
	 select 
		 d.diary_no, d.diary_date, d.diary_context, d.diary_isopen, d.diary_iscomment, 
		 d.diary_image
	 from bookmark b inner join diary d on b.diary_no = d.diary_no
	 where b.user_no = #{no}
	 order by diary_date desc
	</select>
	
</mapper>